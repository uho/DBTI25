{
    "name": "Datenbanktheorie- und Implementierung",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "forwardPorts": [3306],
    "postCreateCommand": "echo 'Codespace bereit!'",
    "remoteUser": "root",

    "customizations": {
        "vscode": {
            "extensions": [
			]
        },
        "settings": { 
            "terminal.integrated.shell.linux": "/bin/bash",
            "git.enabled": true,
            "git.autofetch": true,
            "git.confirmSync": false
        }
    }
}

